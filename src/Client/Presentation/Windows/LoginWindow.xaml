<mah:MetroWindow x:Class="Presentation.Windows.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 mc:Ignorable="d"
                 Icon="../../Assets/Icons/tilia.png"
                 WindowStartupLocation="CenterScreen"
                 Title="Tilia | Inicio de sesión" Height="450" Width="800">
    <Grid x:Name="LoginMainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Background="{StaticResource PrimaryColor}" >
            <Image x:Name="TiliaIcon"
                           Source="/Assets/Ilustrations/DoctorOrgLogin.png"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0" />
        </StackPanel>

        <Border Grid.Column="1">
            <Border.Effect>
                <DropShadowEffect Color="#FF4F4F4F" BlurRadius="7" Opacity="0.4" Direction="-160">
                </DropShadowEffect>
            </Border.Effect>
            <StackPanel Background="White" >
                <Label HorizontalAlignment="Center">Inicio de sesión</Label>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>
